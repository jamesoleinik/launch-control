# Pinned to current pre-release as of 2026-05-08.
# Update with care: the SDK is in beta and surfaces (incl. MCP transport)
# may change between minor versions.
agent-framework-github-copilot>=1.0.0b260507,<1.1
python-dotenv>=1.0
requests>=2.31
rich>=13.7
