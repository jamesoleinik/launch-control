# Required: the Dataverse environment URL.
# The agent shells out to `npx @microsoft/dataverse mcp <DATAVERSE_URL>` which
# uses the cached MSAL token from a prior `dataverse auth create` run.
DATAVERSE_URL=https://yourorg.crm.dynamics.com
